services:
  db:
    image: postgres:15
    container_name: postgres_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: christine
      POSTGRES_PASSWORD: "christinerv4573@4573"
      POSTGRES_DB: vcdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./server
    container_name: fastapi_backend
    depends_on:
      - db
    env_file:
      - .env
    environment:
      VC_DB_URL: "postgresql+psycopg2://christine:christinerv4573%404573@db:5432/vcdb"
    ports:
      - "8000:8000"
    volumes:
      - ./server:/app
    command: ["./wait-for-db.sh", "db", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]

volumes:
  postgres_data:
