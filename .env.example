# MOSIP Offline Verifier - Environment Configuration
# Copy this file to .env (same directory as docker-compose.yml) and update the values below

# =====================================================
# Database Configuration (PostgreSQL)
# =====================================================
POSTGRES_USER=vcuser                                                                # can be anything
POSTGRES_PASSWORD=secure_password_123                                               # can be anything
POSTGRES_DB=vcdb                                                                    # recommend keeping as 'vcdb'

# =====================================================
# Application Configuration
# =====================================================
ADMIN_PASSWORD=admin_secure_password                                                # Admin login password (choose a secure password)

# =====================================================
# Database Connection URL
# =====================================================
# IMPORTANT: Make sure username and password in URL match POSTGRES_USER and POSTGRES_PASSWORD above
VC_DB_URL=postgresql://vcuser:secure_password_123@db:5432/vcdb

# =====================================================
# Security Keys (GENERATE NEW ONES - DO NOT USE EXAMPLES)
# =====================================================
# Generate in terminal using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
VC_STORE_KEY=IpXdwqha2jEm3JJwaql9bR6qGMK6AuNK7rpLwh-vxOE=

# Generate in terminal using: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=f95e7b4df7813db063ec3119f4e61572abc123def456789

# =====================================================
# Database Pool Settings (usually no need to change)
# =====================================================
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=1800